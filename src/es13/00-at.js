const array = ["one", "two", "three", "four", "five"];
// para acceder al ultimo elemento
const lastElement = array[array.length - 1];
console.log(lastElement);
//con at
const newElement = array.at(-1);
console.log(newElement);