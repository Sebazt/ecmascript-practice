let hello = "Hi";
let world = "World";

//template literals
let newFeature = `${hello} ${world}, this is the new feature for strings`
console.log(newFeature);

//multi-line strings

let lorem = `${hello} this is a test
and this is the continue of my test
`
console.log(lorem);
